        # process to do this question 
        # first find minimum of (A-1, B-1), (8-A, B-1), (A-1, 8-B), and (8-A, 8-B)
        # after that sum all and print 
class Solution:
    def solve(self, A, B):
        one=[A-1, B-1]
        two= [8-A, B-1]
        three=[A-1, 8-B]
        four=[8-A, 8-B]
        
        final_ans=min(one)+min(two)+min(three)+min(four)
        return final_ans


